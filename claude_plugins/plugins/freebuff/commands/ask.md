---
description: Ask Freebuff a single question or one-shot task using free cloud inference.
argument_names:
  - prompt
---

Run a one-shot query or coding prompt using Freebuff:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/freebuff-run.sh" ask "$ARG_PROMPT" "$(pwd)"
```
