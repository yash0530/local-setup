{
  "name": "local-llm",
  "version": "1.0.0",
  "description": "Opt-in access to the locally-served Qwen 3.6 models (llama-server). Bundles the `local-qwen` delegation subagent and the `local-llm` reference skill. Deliberately NOT installed into ~/.claude — it is loaded per-session via `claude_local_subagent` so a plain `claude` never sees it.",
  "author": {
    "name": "yash0530"
  }
}
